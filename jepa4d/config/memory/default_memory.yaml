active_local_map:
  radius_m: 5.0
  frame_id: base_link
  stale_after_s: 30.0
  max_observations: 256
global_scene_graph:
  frame_id: map
  max_history_per_object: 512
confidence_half_life_seconds: 3600
vector_backend: in_memory
persistence:
  backend: sqlite
  schema_version: 2
  journal_mode: WAL
  snapshot_every_updates: 1
lod:
  max_object_history: 32
  max_events: 256
  max_local_observations: 64
